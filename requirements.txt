# --- PyTorch and Core Dependencies ---
# Note: torch 2.6.0+cu124 is specified.
# Note: torchvision 0.12.0 is not compatible with PyTorch 2.6.0.
# If you run into installation or compatibility issues, consider using torchvision==0.21.0+cu124.
# Note: numpy 1.19.2 does not support Python 3.10.
# If installation fails or has conflicts with torch 2.6.0, consider using numpy>=1.22.0 (e.g., numpy==1.23.5).

# --- Package Dependencies ---
diffusers
bezier
opencv-python
pudb

invisible-watermark
imageio==2.9.0
imageio-ffmpeg==0.4.2
pytorch-lightning==1.4.2
omegaconf==2.1.1
test-tube>=0.7.5
streamlit>=0.73.1
einops==0.3.0
torch-fidelity==0.3.0
transformers==4.19.2
torchmetrics==0.6.0
kornia==0.6

# --- Git Repositories ---
-e git+https://github.com/CompVis/taming-transformers.git@master#egg=taming-transformers
-e git+https://github.com/openai/CLIP.git@main#egg=clip
